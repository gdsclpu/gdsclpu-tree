# GDSC - LPU Tree

## Contributing

- Install dependencies

  ```bash
  yarn
  ```

- Run development server

  ```bash
  yarn dev
  ```

### With Docker

1. Build the image
   ```sh
   docker-compose build
   ```
2. Run the docker container
   ```sh
   docker-compose up
   ```
